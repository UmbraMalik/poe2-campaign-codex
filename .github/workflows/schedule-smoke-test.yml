name: Schedule smoke test

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Print event and time
        run: |
          echo "event=${{ github.event_name }}"
          date -u
